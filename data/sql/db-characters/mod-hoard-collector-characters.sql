DROP TABLE IF EXISTS `mod_collector_items`;
CREATE TABLE `mod_collector_items` (
`PlayerGUID` INT UNSIGNED NOT NULL,
`StorageID` TINYINT DEFAULT 0,
`ItemEntry` INT UNSIGNED NOT NULL,
`ItemGUID` INT UNSIGNED NOT NULL
);
